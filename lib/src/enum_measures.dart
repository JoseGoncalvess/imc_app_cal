
enum Measureswidth{
kg(name:"KG"),
g(name:"G");
const Measureswidth({required this.name});
final String name;

}

enum Measuresheight{
  cm(name: "CM"),
  m(name: "M");

  const Measuresheight({required this.name});
  final String name;
}

